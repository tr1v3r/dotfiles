diff --git a/lib/types/components/CommandSuggestions.d.ts b/lib/types/components/CommandSuggestions.d.ts
index 9cfdc0a..a506cbd 100644
--- a/lib/types/components/CommandSuggestions.d.ts
+++ b/lib/types/components/CommandSuggestions.d.ts
@@ -31,5 +31,7 @@ export declare function CommandSuggestions({ commands, selectedIndex, columns, q
     onPick?: (index: number) => void;
     /** 滚轮步进（fullscreen）：±1 移动选中行。 */
     onWheelStep?: (step: 1 | -1) => void;
+    /** 覆盖卡片标题（默认「命令」）——行中技能手势浮层传入技能标题。 */
+    title?: string;
 }): React.ReactNode;
 //# sourceMappingURL=CommandSuggestions.d.ts.map
\ No newline at end of file
diff --git a/lib/types/components/CommandSuggestions.js b/lib/types/components/CommandSuggestions.js
index 5c2b430..665e2c8 100644
--- a/lib/types/components/CommandSuggestions.js
+++ b/lib/types/components/CommandSuggestions.js
@@ -21,7 +21,7 @@ import { SuggestionCard, cardContentWidth, splitQueryMatch } from './SuggestionC
  * （bold 与 dim 在终端互斥，故提亮用非 dim 而非加粗）；选中行整行
  * suggestion 色、名字加粗、前置 ❯ 指针。
  */
-export function CommandSuggestions({ commands, selectedIndex, columns, query = '', accent, onPick, onWheelStep, }) {
+export function CommandSuggestions({ commands, selectedIndex, columns, query = '', accent, onPick, onWheelStep, title, }) {
     if (commands.length === 0)
         return null;
     // Cap the command name column at 40% of the card's content width to ensure
@@ -36,7 +36,9 @@ export function CommandSuggestions({ commands, selectedIndex, columns, query = '
     const below = commands.length - (startIndex + visible.length);
     // 前缀高亮取查询的最后一段 token（`/plan of` 高亮 `of` 命中的段）。
     const queryToken = query.replace(/^\//, '').match(/[^ \t]*$/)?.[0] ?? '';
-    const title = `${t('sugg-commands-title')} · ${t('sugg-count', { n: commands.length })}`;
+    // `title` overrides the heading for non-command callers (the mid-line
+    // skill gesture overlay passes the skills picker's own label).
+    const heading = title ?? t('sugg-commands-title');
     const footer = above > 0 || below > 0
         ? [
             above > 0 ? t('sugg-more-above', { n: above }) : null,
@@ -45,7 +47,7 @@ export function CommandSuggestions({ commands, selectedIndex, columns, query = '
             .filter((part) => part !== null)
             .join(' · ')
         : null;
-    return (_jsx(SuggestionCard, { title: title, columns: columns, accent: accent, footer: footer, onRowPick: onPick ? index => onPick(startIndex + index) : undefined, onWheelStep: onWheelStep, rows: visible.map(command => {
+    return (_jsx(SuggestionCard, { title: `${heading} · ${t('sugg-count', { n: commands.length })}`, columns: columns, accent: accent, footer: footer, onRowPick: onPick ? index => onPick(startIndex + index) : undefined, onWheelStep: onWheelStep, rows: visible.map(command => {
             const isSelected = command.name === commands[selectedIndex]?.name;
             const tagText = command.tag ? `[${command.tag}] ` : '';
             const tagWidth = stringWidth(tagText);
diff --git a/lib/types/components/PromptInput.js b/lib/types/components/PromptInput.js
index f7744a7..5d47661 100644
--- a/lib/types/components/PromptInput.js
+++ b/lib/types/components/PromptInput.js
@@ -4,6 +4,7 @@ import stripAnsi from 'strip-ansi';
 import { readFile, unlink } from 'node:fs/promises';
 import { basename } from 'node:path';
 import { t } from '../i18n.js';
+import { vimCanonicalKey } from '../utils/keymap.js';
 import { Box, Text, useInput, useTerminalSize, useTheme } from '../ui.js';
 import { EffortChargeGlyph } from './EffortChargeGlyph.js';
 import { EffortInputBorder } from './EffortInputBorder.js';
@@ -146,6 +147,20 @@ function vimWordEnd(text, cursor) {
         i++;
     return i;
 }
+/** End of the current OR NEXT word (`e` motion): from mid-word, this word's
+ *  end; from a word end or whitespace, skip ahead and take the next word's
+ *  end — repeated presses walk the word ends (unlike `vimWordEnd`, which
+ *  stalls on the whitespace a previous press landed on). */
+function vimWordEndForward(text, cursor) {
+    const len = text.length;
+    let i = cursor;
+    if (i < len && /\s/.test(text[i]))
+        while (i < len && /\s/.test(text[i]))
+            i++;
+    while (i < len && !/\s/.test(text[i]))
+        i++;
+    return i;
+}
 // --- grapheme-cluster geometry ---------------------------------------------
 // The caret, editing keys, and wrapping MUST agree on one text unit. Mixing
 // UTF-16 code units (arrows/backspace), code points (wrap), and display
@@ -249,6 +264,50 @@ const EDITOR_CHROME_ROWS = 5;
  * carries onDragStart), so the prompt detects the double-click itself.
  */
 const DOUBLE_CLICK_MS = 500;
+/**
+ * Mid-line skill gesture (`/name`) at the caret. The kernel's pre-step hook
+ * (dsh-tool-skill's SKILL_GESTURE) injects EVERY whitespace-bounded `/name`
+ * token in a user message, so skills are invocable mid-sentence, not only as
+ * the line's first token — this finds the gesture token the caret sits in,
+ * for completion (same caret-token shape as the @ mention, issue #15). The
+ * FIRST token (offset 0) is deliberately excluded: that is the command
+ * overlay's turf (`value.startsWith('/')`), whose Enter RUNS the command —
+ * mid-line gestures are insert-only. Name chars mirror SKILL_GESTURE's
+ * grammar; the query may be a partial ('' right after the `/`).
+ */
+const skillGestureAtCaret = (value, cursor) => {
+    // Token start: scan back from the caret to the previous boundary.
+    let start = cursor;
+    while (start > 0 && !/\s/.test(value[start - 1]))
+        start--;
+    if (start === 0 || value[start] !== '/')
+        return undefined;
+    // Token end: first boundary at/after the caret.
+    let end = cursor;
+    while (end < value.length && !/\s/.test(value[end]))
+        end++;
+    // The COMPLETE token must be a gesture — a `/usr/bin` path is not one
+    // (SKILL_GESTURE's name grammar has no inner `/`), so it completes
+    // nothing. An empty body (just typed `/`) lists everything.
+    const body = value.slice(start + 1, end);
+    if (body !== '' && !/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(body))
+        return undefined;
+    return { start, end, query: value.slice(start + 1, cursor) };
+};
+/**
+ * Every mid-line `/name` gesture in the text (SKILL_GESTURE's own shape; `m`
+ * so a token may open a later line), as [start, end) ranges. Offset 0 is
+ * excluded for the same reason as in skillGestureAtCaret.
+ */
+const skillGestureRanges = (text) => {
+    const ranges = [];
+    for (const match of text.matchAll(/(^|\s)(\/[a-z0-9]+(?:-[a-z0-9]+)*)(?=\s|$)/gm)) {
+        const start = match.index + match[1].length;
+        if (start > 0)
+            ranges.push([start, start + match[2].length]);
+    }
+    return ranges;
+};
 /**
  * Claude Code style prompt input: rounded border box (top+bottom borders
  * only), `❯ ` prompt char (dimmed while a turn is working), the text with a
@@ -548,6 +607,37 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
         !helpOpen &&
         !selectionActive &&
         fileEscRef.current !== mention?.start;
+    // Mid-line skill gesture completion (SKILL_GESTURE parity, @-mention
+    // architecture): candidates come synchronously from the merged command
+    // list — user-invocable skills are already in there with the `skill`
+    // marker (the channel folds the registry catalog in on skills/change),
+    // so unlike the @ overlay there is no async fetch to own. Mid-line
+    // suggestions list SKILLS ONLY: a mid-line `/name` is adjudicated by the
+    // kernel gesture scan (a registry lookup), never dispatched as a command,
+    // so offering /model & co. here would mislead.
+    const [skillSelected, setSkillSelected] = React.useState(0);
+    const skillGesture = skillGestureAtCaret(value, cursor);
+    const skillMatches = skillGesture === undefined
+        ? []
+        : channel.commandList
+            .filter(command => command.skill && command.name.startsWith(skillGesture.query))
+            .slice(0, 50);
+    // Esc dismisses the overlay for the token being edited (it reopens once
+    // the text changes); it must NOT clear a mid-message input.
+    const skillEscRef = React.useRef(-1);
+    React.useEffect(() => {
+        skillEscRef.current = -1;
+    }, [value]);
+    const skillOverlayOpen = skillMatches.length > 0 &&
+        !expanded &&
+        !helpOpen &&
+        !selectionActive &&
+        !overlayOpen &&
+        !fileOverlayOpen &&
+        skillEscRef.current !== skillGesture?.start;
+    React.useEffect(() => {
+        setSkillSelected(0);
+    }, [skillGesture?.start, skillGesture?.query]);
     /** Fold-block state + a synchronous mirror (setInput reads the ref).
      *  Creating a block also drags a caret that sits inside it out to the
      *  block's end (the block is atomic; typing continues after it). */
@@ -670,6 +760,22 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
         setInput(next, mention.start + insert.length);
         setFileSelected(0);
     };
+    /**
+     * Accept a skill suggestion: replace ONLY the gesture token at the caret
+     * (`/do` → `/dot-skill`), keeping the surrounding line untouched. The
+     * line is NOT submitted — mid-line gestures are insert-only; running is
+     * the first-token command overlay's semantics. A trailing space is added
+     * only at end-of-input (a following token already brings its own
+     * separator), so completions never stack double spaces.
+     */
+    const acceptSkill = (skill) => {
+        if (skillGesture === undefined)
+            return;
+        const insert = skillGesture.end >= value.length ? `/${skill.name} ` : `/${skill.name}`;
+        const next = value.slice(0, skillGesture.start) + insert + value.slice(skillGesture.end);
+        setInput(next, skillGesture.start + insert.length);
+        setSkillSelected(0);
+    };
     const submitText = (text, notice) => {
         const trimmed = text.trim();
         if (!trimmed)
@@ -838,6 +944,16 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
                 return;
             }
         }
+        // Skill-gesture overlay open → accept the selection into the line
+        // (same contract as the file menu: the overlay owns Enter while open;
+        // the line is submitted only once the overlay closes).
+        if (skillOverlayOpen) {
+            const skill = skillMatches[skillSelected];
+            if (skill) {
+                acceptSkill(skill);
+                return;
+            }
+        }
         if (channel.working && value.trim() !== '') {
             // CC's immediate-command semantics: /btw and /skills are exempt from
             // steering — neither command interrupts the running turn. Hidden
@@ -947,7 +1063,7 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
         // ONLY drops the highlight (text untouched), and Backspace/Delete
         // delete the selected span. Arrows/typing handle the selection at their
         // own arms below.
-        if (key.escape && selection && !helpOpen && !overlayOpen && !fileOverlayOpen) {
+        if (key.escape && selection && !helpOpen && !overlayOpen && !fileOverlayOpen && !skillOverlayOpen) {
             event.stopImmediatePropagation();
             clearSelection();
             return;
@@ -1246,6 +1362,12 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
                 acceptFile(file);
             return;
         }
+        if (key.tab && skillOverlayOpen) {
+            const skill = skillMatches[skillSelected];
+            if (skill)
+                acceptSkill(skill);
+            return;
+        }
         if (key.tab && overlayOpen) {
             const command = suggestions[selectedCommand];
             if (command) {
@@ -1309,6 +1431,10 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
                 setFileSelected(index => index <= 0 ? fileMatches.length - 1 : index - 1);
                 return;
             }
+            if (skillOverlayOpen && historyIndex.current < 0) {
+                setSkillSelected(index => index <= 0 ? skillMatches.length - 1 : index - 1);
+                return;
+            }
             // With a fold block the caret in the tail walks the TAIL's lines
             // (the block is one atomic row); the first tail line steps over the
             // block to its head.
@@ -1358,6 +1484,10 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
                 setFileSelected(index => index >= fileMatches.length - 1 ? 0 : index + 1);
                 return;
             }
+            if (skillOverlayOpen && historyIndex.current < 0) {
+                setSkillSelected(index => index >= skillMatches.length - 1 ? 0 : index + 1);
+                return;
+            }
             // Mirror of the tail-side ↑ walk: with a fold block the caret walks
             // the TAIL's lines; past its last line it falls through to the
             // overlay/history handling below.
@@ -1541,6 +1671,10 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
             const bounds = graphemeBoundaries(value);
             const pending = vimPendingRef.current;
             vimPendingRef.current = '';
+            // Vim key overrides (settings `dsh-tui.vimKeys`) translate before
+            // both dispatches so the `d` operator's second key follows the
+            // same layout as the motion it names.
+            input = vimCanonicalKey(input);
             // Operator pending (`d`): the second key picks the target.
             if (pending === 'd') {
                 switch (input) {
@@ -1620,6 +1754,9 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
                 case 'b':
                     setInput(value, vimWordBackward(value, cursor));
                     return;
+                case 'e': // forward to end of word (vim `e`)
+                    setInput(value, vimWordEndForward(value, cursor));
+                    return;
                 case 'x': { // delete the character at the caret; at the very end of
                     // the text delete the last character (vim: the caret sits ON the
                     // last char after `$`, so `x` must still delete it). Same rule
@@ -1705,14 +1842,14 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
         if (vimEnabledRef.current) {
             if (vimInsertRef.current) {
                 // INSERT: Esc returns to NORMAL instead of the clear/rewind path.
-                if (key.escape && !helpOpen && !overlayOpen && !fileOverlayOpen) {
+                if (key.escape && !helpOpen && !overlayOpen && !fileOverlayOpen && !skillOverlayOpen) {
                     event.stopImmediatePropagation();
                     vimInsertRef.current = false;
                     setVimInsert(false);
                     return;
                 }
             }
-            else if (!helpOpen && !overlayOpen && !fileOverlayOpen) {
+            else if (!helpOpen && !overlayOpen && !fileOverlayOpen && !skillOverlayOpen) {
                 if (key.escape) {
                     // NORMAL: Esc cancels a pending operator and otherwise no-ops
                     // (the clear/rewind double-Esc semantics belong to non-vim mode).
@@ -1782,6 +1919,12 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
                 fileEscRef.current = mention?.start ?? -1;
                 return;
             }
+            // Skill overlay: same token-scoped dismissal — clearing the input
+            // would nuke the surrounding mid-message text.
+            if (skillOverlayOpen) {
+                skillEscRef.current = skillGesture?.start ?? -1;
+                return;
+            }
             // With pending messages while working, Esc = interrupt and deliver
             // them right away (Codex's "interrupt and send immediately"): the
             // turn is aborted and each message is re-queued once it settles.
@@ -1971,6 +2114,13 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
             ...visualLineRanges(tail, inputWidth).map(([s, e]) => [s + block.end, e + block.end]),
         ]
         : visualLineRanges(value, inputWidth);
+    // Mid-line skill gestures that name a KNOWN user-invocable skill, as
+    // [start, end) ranges — highlighted in the prompt so a line like
+    // `用 /commit 和 /dot-skill 处理` reads the way the kernel will
+    // adjudicate it. Unknown names stay plain (the pre-step scan skips them
+    // silently too), and offset 0 keeps its command affordances instead.
+    const skillNameSet = new Set(channel.commandList.filter(command => command.skill).map(command => command.name));
+    const skillTokenRanges = skillGestureRanges(value).filter(([start, end]) => skillNameSet.has(value.slice(start + 1, end)));
     /**
      * Inverse runs for one rendered row, shared by the inline prompt and the
      * expanded editor: the selection's intersection (if any) and the caret
@@ -1981,6 +2131,10 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
      * emoji as one glyph; at the text end it shows a blank inverse cell like
      * the empty-input caret (appended after everything, so a selection
      * ending there cannot swallow it).
+     *
+     * Mid-line skill gesture tokens intersecting the row render in the
+     * prompt accent color (see skillTokenRanges) — inverse wins on overlap,
+     * so a selection or the caret block inside a gesture stays legible.
      */
     const rowHighlightPieces = (text, absoluteLine) => {
         const intervals = [];
@@ -2010,18 +2164,41 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
             else
                 runs.push([s, e]);
         }
-        const pieces = [];
-        let pos = 0;
+        // Skill-gesture tokens crossing this row, clipped to row-local
+        // [lo, hi) through the SAME row-range mapping the selection uses.
+        const accents = [];
+        if (skillTokenRanges.length > 0) {
+            const [rowStart] = lineRanges[absoluteLine] ?? [0, 0];
+            for (const [gStart, gEnd] of skillTokenRanges) {
+                const lo = Math.min(Math.max(gStart - rowStart, 0), text.length);
+                const hi = Math.min(Math.max(gEnd - rowStart, 0), text.length);
+                if (hi > lo)
+                    accents.push([lo, hi]);
+            }
+        }
+        // Partition the row at every interval edge; a segment is inverse
+        // when inside a run, else accent when inside a gesture, else plain.
+        const inInterval = (list, at) => list.some(([s, e]) => at >= s && at < e);
+        const cuts = new Set([0, text.length]);
         for (const [s, e] of runs) {
-            if (s > pos)
-                pieces.push({ text: text.slice(pos, s), inverse: false });
-            pieces.push({ text: text.slice(s, e), inverse: true });
-            pos = Math.max(pos, e);
+            cuts.add(s);
+            cuts.add(e);
+        }
+        for (const [s, e] of accents) {
+            cuts.add(s);
+            cuts.add(e);
+        }
+        const points = [...cuts].sort((a, b) => a - b);
+        const pieces = [];
+        for (let i = 0; i + 1 < points.length; i++) {
+            const from = points[i];
+            const to = points[i + 1];
+            const inverse = inInterval(runs, from);
+            const accent = !inverse && inInterval(accents, from);
+            pieces.push({ text: text.slice(from, to), inverse, accent });
         }
-        if (pos < text.length)
-            pieces.push({ text: text.slice(pos), inverse: false });
         if (endBlankCaret)
-            pieces.push({ text: ' ', inverse: true });
+            pieces.push({ text: ' ', inverse: true, accent: false });
         return pieces;
     };
     const rendered = visibleLines.map((line, index) => {
@@ -2038,7 +2215,7 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
         const text = withPrefix ? truncateToWidth(line, inputWidth - prefixCols) : line;
         const prefix = withPrefix ? _jsx(Text, { dimColor: true, children: prefixLabel }) : null;
         const pieces = rowHighlightPieces(text, absoluteLine);
-        return (_jsxs(Text, { wrap: "truncate-end", children: [prefix, pieces.length === 0 ? ' ' : pieces.map((piece, pieceIndex) => piece.inverse ? (_jsx(Text, { inverse: true, children: piece.text }, pieceIndex)) : (piece.text))] }, absoluteLine));
+        return (_jsxs(Text, { wrap: "truncate-end", children: [prefix, pieces.length === 0 ? ' ' : pieces.map((piece, pieceIndex) => piece.inverse ? (_jsx(Text, { inverse: true, children: piece.text }, pieceIndex)) : piece.accent ? (_jsx(Text, { color: promptAccent, bold: true, children: piece.text }, pieceIndex)) : (piece.text))] }, absoluteLine));
     });
     // ── 展开态编辑行 ────────────────────────────────────────────────────
     // Logical line number per visual row (a wrapped continuation keeps its
@@ -2063,7 +2240,7 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
                 ? ' '.repeat(editorNoWidth)
                 : String(logicalNo + 1).padStart(editorNoWidth, ' ');
             const pieces = rowHighlightPieces(line, absoluteLine);
-            return (_jsxs(Text, { wrap: "truncate-end", backgroundColor: isCaretRow ? 'toolCardBackgroundDim' : undefined, children: [_jsx(Text, { dimColor: !isCaretRow, bold: isCaretRow, color: isCaretRow ? promptAccent : undefined, children: `${gutterLabel} │ ` }), pieces.map((piece, pieceIndex) => piece.inverse ? (_jsx(Text, { inverse: true, children: piece.text }, pieceIndex)) : (piece.text))] }, absoluteLine));
+            return (_jsxs(Text, { wrap: "truncate-end", backgroundColor: isCaretRow ? 'toolCardBackgroundDim' : undefined, children: [_jsx(Text, { dimColor: !isCaretRow, bold: isCaretRow, color: isCaretRow ? promptAccent : undefined, children: `${gutterLabel} │ ` }), pieces.map((piece, pieceIndex) => piece.inverse ? (_jsx(Text, { inverse: true, children: piece.text }, pieceIndex)) : piece.accent ? (_jsx(Text, { color: promptAccent, bold: true, children: piece.text }, pieceIndex)) : (piece.text))] }, absoluteLine));
         })
         : null;
     // Peek card content: the BLOCK's text wrapped to the card's inner width,
@@ -2295,7 +2472,7 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
     // 覆盖的转录行会留空（见 Chat.tsx dialogOverlayOpen 注释）。展开态由
     // 全屏编辑器接管，内联浮层全部撤下。
     const floatersOpen = !expanded &&
-        (helpOpen || channel.pending.length > 0 || fileOverlayOpen || overlayOpen || peekOpen);
+        (helpOpen || channel.pending.length > 0 || fileOverlayOpen || skillOverlayOpen || overlayOpen || peekOpen);
     // 顶边框右侧的会话名标签（CC 风格 chip）：色随强调色；超宽截断，宽度
     // 随终端列数伸缩但不超过 28 显示单元。默认关闭——`/settings` 的
     // 「会话名标签」开关（dsh-tui.promptSessionLabel）开启后显示。
@@ -2396,7 +2573,17 @@ export function PromptInput({ channel, helpOpen, onToggleHelp, onRunCommand, sel
                         // 滚轮 = 移动选中行（与 ↑/↓ 同路径，窗口跟随）
                         onWheelStep: (step) => {
                             setFileSelected(i => Math.max(0, Math.min(fileMatches.length - 1, i + step)));
-                        } })), overlayOpen && (_jsx(CommandSuggestions, { commands: suggestions, selectedIndex: selectedCommand, columns: columns, query: value, accent: promptAccent, 
+                        } })), skillOverlayOpen && (_jsx(CommandSuggestions, { commands: skillMatches, selectedIndex: skillSelected, columns: columns, query: `/${skillGesture?.query ?? ''}`, accent: promptAccent, title: t('picker-title-skills'),
+                        // 点击行 = 接受该项（与 Enter 同路径，只替换手势 token）
+                        onPick: (index) => {
+                            const skill = skillMatches[index];
+                            if (skill)
+                                acceptSkill(skill);
+                        },
+                        // 滚轮 = 移动选中行（与 ↑/↓ 同路径，窗口跟随）
+                        onWheelStep: (step) => {
+                            setSkillSelected(i => Math.max(0, Math.min(skillMatches.length - 1, i + step)));
+                        } })), overlayOpen && (_jsx(CommandSuggestions, { commands: suggestions, selectedIndex: selectedCommand, columns: columns, query: value, accent: promptAccent,
                         // 点击行 = 运行该命令（与 Enter 同路径）
                         onPick: (index) => {
                             const command = suggestions[index];
diff --git a/lib/types/dsh-adapter/plugin.js b/lib/types/dsh-adapter/plugin.js
index 15a6f44..5a64860 100644
--- a/lib/types/dsh-adapter/plugin.js
+++ b/lib/types/dsh-adapter/plugin.js
@@ -24,7 +24,7 @@ import { resolveSessionCwd } from '../utils/workspaceRoot.js';
 import { beginRestartAttempt, checkForTuiUpdate, installedTuiVersion, isBootDeadlockTarget, isStandaloneRuntime, isVersionNewer, logRestartEvent, resolveDshProfileName, resolveTuiUpdateTarget, restartTui, updateTuiAndRestart, writeHandoffNotice } from '../update.js';
 import { getLang, isLang, resolveStartupLang, setLang, t, writeLangPref } from '../i18n.js';
 import { DEFAULT_PAGE_MARGIN, DEFAULT_STATUS_BAR, applyPageMargin, isPageMarginMode, normalizePageMargin, normalizeScrollGutter, normalizeStatusBar, normalizeToolBackground, parsePageMarginSpec } from '../tuiDisplayPrefs.js';
-import { draftComboConflicts, effectiveComboString, parseComboDraft, setKeymapOverrides, SHORTCUT_ACTIONS, } from '../utils/keymap.js';
+import { draftComboConflicts, effectiveComboString, parseComboDraft, setKeymapOverrides, setVimKeys, SHORTCUT_ACTIONS, VIM_ACTIONS, } from '../utils/keymap.js';
 import { detectLegacyEnv, migrateLegacyDataDir, RENAMED_ENV } from '../utils/paths.js';
 import { attachHerdrIntegration } from '../herdr.js';
 import { logMouseDebug } from '../utils/debug.js';
@@ -577,6 +577,11 @@ export async function apply(ctx, config) {
             // action (see src/utils/keymap.ts). Unset keeps the default binding
             // and the section's format() shows the effective combos.
             shortcuts: Schema.object(Object.fromEntries(SHORTCUT_ACTIONS.map(action => [action.id, Schema.string().required(false)]))).required(false),
+            // `/vim` normal-mode key overrides, one optional single-character
+            // key per action (see VIM_ACTIONS in src/utils/keymap.ts). Unset
+            // keeps the default key; an override REPLACES the default key's
+            // meaning when it lands on another action's key.
+            vimKeys: Schema.object(Object.fromEntries(VIM_ACTIONS.map(action => [action.id, Schema.string().required(false)]))).required(false),
         }));
         const applyLayout = (value) => {
             channel.setDiffLayout(value.diffLayout ?? config.diffLayout ?? 'auto');
@@ -644,6 +649,23 @@ export async function apply(ctx, config) {
             }
             setKeymapOverrides(merged);
         };
+        // Vim key overrides resolve like shortcuts: settings user layer over
+        // cordis.yml, unset everywhere keeps the registry default. Applied
+        // live into the keymap module — the next keypress already uses the
+        // new keys.
+        const applyVimKeys = (value) => {
+            const userLayer = value.vimKeys ?? {};
+            const configLayer = config.vimKeys ?? {};
+            const merged = {};
+            for (const action of VIM_ACTIONS) {
+                const chosen = typeof userLayer[action.id] === 'string' && userLayer[action.id] !== ''
+                    ? userLayer[action.id]
+                    : (typeof configLayer[action.id] === 'string' && configLayer[action.id] !== '' ? configLayer[action.id] : undefined);
+                if (chosen !== undefined)
+                    merged[action.id] = chosen;
+            }
+            setVimKeys(merged);
+        };
         const apply = (next) => {
             applyLayout(next);
             applyWhale(next);
@@ -651,6 +673,7 @@ export async function apply(ctx, config) {
             applyLang(next);
             applyDisplay(next);
             applyShortcuts(next);
+            applyVimKeys(next);
             applyFullscreen(next);
         };
         // One-time fullscreen factory-default migration (companion to the
diff --git a/lib/types/utils/keymap.js b/lib/types/utils/keymap.js
index ecd006c..cc64b8e 100644
--- a/lib/types/utils/keymap.js
+++ b/lib/types/utils/keymap.js
@@ -326,3 +326,71 @@ export function reservedActionCombos() {
     }
     return reserved;
 }
+// --- vim normal-mode keys -----------------------------------------------------
+// `/vim`'s NORMAL submode binds bare characters, not ctrl/alt combos, so it
+// gets its own action registry beside SHORTCUT_ACTIONS: same shape (id +
+// defaults), same live-override cache fed by plugin.ts from the settings
+// user layer over cordis.yml. PromptInput translates a pressed key to the
+// canonical built-in key before its dispatch, so one remap covers motions,
+// edits, the `d` operator's second key, and the insert entries alike.
+/** Identifiers of every customizable `/vim` normal-mode action. */
+export const VIM_ACTIONS = [
+    { id: 'left', defaults: ['h'] },
+    { id: 'down', defaults: ['j'] },
+    { id: 'up', defaults: ['k'] },
+    { id: 'right', defaults: ['l'] },
+    { id: 'lineStart', defaults: ['0'] },
+    { id: 'firstNonBlank', defaults: ['^'] },
+    { id: 'lineEnd', defaults: ['$'] },
+    { id: 'wordForward', defaults: ['w'] },
+    { id: 'wordBackward', defaults: ['b'] },
+    { id: 'wordEnd', defaults: ['e'] },
+    { id: 'deleteChar', defaults: ['x'] },
+    { id: 'deleteBefore', defaults: ['X'] },
+    { id: 'deleteOperator', defaults: ['d'] },
+    { id: 'undo', defaults: ['u'] },
+    { id: 'insert', defaults: ['i'] },
+    { id: 'insertFirstNonBlank', defaults: ['I'] },
+    { id: 'insertAfter', defaults: ['a'] },
+    { id: 'insertEndOfLine', defaults: ['A'] },
+    { id: 'openBelow', defaults: ['o'] },
+    { id: 'openAbove', defaults: ['O'] },
+];
+/** Canonical built-in key of each action — overrides point an action at a
+ *  NEW pressed key, so the translation below maps back to this key. */
+const CANONICAL_VIM_KEY = new Map(VIM_ACTIONS.map(action => [action.id, action.defaults[0]]));
+/** Live vim overrides: pressed key → action id (settings user layer +
+ *  cordis.yml merged by plugin.ts). */
+let vimOverrideMap = new Map();
+/**
+ * Replace the whole vim override map. Values must be single printable
+ * characters (case-sensitive); anything else is DROPPED and the action
+ * keeps its default — same typo rule as setKeymapOverrides. `/` and `?`
+ * are refused: they own the command menu / help shortcuts before the vim
+ * dispatch ever sees them.
+ */
+export function setVimKeys(overrides) {
+    const next = new Map();
+    for (const action of VIM_ACTIONS) {
+        const raw = overrides[action.id];
+        if (raw === undefined)
+            continue;
+        for (const entry of (Array.isArray(raw) ? raw : [raw])) {
+            const key = String(entry);
+            if (key.length !== 1 || key === '/' || key === '?')
+                continue;
+            next.set(key, action.id);
+        }
+    }
+    vimOverrideMap = next;
+}
+/** Test seam: drop every vim override. */
+export function resetVimKeys() {
+    vimOverrideMap = new Map();
+}
+/** The canonical built-in key a pressed vim key acts as; identity for
+ *  unmapped keys (an untouched layout needs no translation). */
+export function vimCanonicalKey(key) {
+    const action = vimOverrideMap.get(key);
+    return action === undefined ? key : (CANONICAL_VIM_KEY.get(action) ?? key);
+}
